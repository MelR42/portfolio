image: node:14

stages:
  - deploy

pages:
  stage: deploy
  script:
    - npm install
    - npm run build 
    - mkdir -p public
    - cp -r dist/* public/ 
  artifacts:
    paths:
      - public
  only:
    - main
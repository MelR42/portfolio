image: node:14

stages:
  - deploy

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp -r Comprovativos public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  only:
    - main